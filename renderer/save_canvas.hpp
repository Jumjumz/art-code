#pragma once

#include <glm/glm.hpp>
#include <vulkan/vulkan_raii.hpp>

// NOTE:for future use, where image can be save in different image format
class SaveCanvas {
  public:
    SaveCanvas();

    void save_canvas();

  private:
};
